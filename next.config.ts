import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["raw.githubusercontent.com", "img.pokemondb.net"], // tambahkan domain gambar yang kamu pakai
  },
};

export default nextConfig;
